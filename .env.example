PORT=3000
NODE_ENV=development
DATABASE_URL=postgres://usuario:password@localhost:5432/bioptica
JWT_SECRET=cambia-este-valor-por-uno-seguro-y-largo

# Envío de fórmulas por correo (opcional). Si no se configuran,
# el botón de enviar por correo devuelve un error claro en vez de fallar en silencio.
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM=
